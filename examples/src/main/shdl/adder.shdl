@package = ScalaHDLExample.SHDL.Arithmetic.Add
@GeAdder

defmod Adder(clk, a, b, z) {
  @sync(1) 
  adder {
    z := a + b
  }
}

@main {
  clk = bool(0)
  a = signed(0, 5)
  b = signed(0, 5)
  z = signed(0, 6)

  compile(Adder(clk, a, b, z), "Adder.v")
}
